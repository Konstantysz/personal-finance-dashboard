# Mapping of categories that changed names over 4 years of exports.
# Without this, year-over-year comparisons are worthless.
# The agent proposes entries during /waliduj; you approve them manually.
#
# format:
#   canonical_name:
#     - old_name_1
#     - old_name_2

mapowanie: {}

# Categories deliberately excluded from consumer spending analysis
# (e.g., technical transfer categories).
wykluczone:
  - "Przelew, wypłata"
